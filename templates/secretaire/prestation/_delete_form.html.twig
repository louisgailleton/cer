<form method="post" action="{{ path('secretaire.supprPrestation', {'id': prestation.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette prestation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
