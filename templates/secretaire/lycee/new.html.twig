{% extends 'base.html.twig' %}

{% block title %}Lycée{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un lycée</h1>

        {{ include('secretaire/lycee/_form.html.twig') }}

        <a href="{{ path('secretaire.lycee') }}" class="btn btn-primary">Retour à la liste</a>
    </div>
{% endblock %}
